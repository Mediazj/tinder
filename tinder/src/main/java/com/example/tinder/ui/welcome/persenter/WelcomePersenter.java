package com.example.tinder.ui.welcome.persenter;

import com.example.tinder.mvp.BasePersenter;
import com.example.tinder.mvp.BaseView;

public class WelcomePersenter extends BasePersenter {
    @Override
    public void bindView(BaseView view) {
        super.bindView(view);
    }
}
